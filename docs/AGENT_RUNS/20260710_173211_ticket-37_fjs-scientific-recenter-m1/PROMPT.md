# Prompt

Operate as the sole FJS canonical writer for the first bounded implementation
milestone from pinned commit `193a325dc681ebc4da67b44715a92e4f63113019`
in an SSD-contained worktree. Do not alter the canonical dirty worktree.

Implement the accepted ambitious recenter without a broad or memory-heavy run:

1. Freeze a canonical design with the exact detector stop-line, real-data
   manifests/universe/dates, baseline ladder, holdout policy, and claim reducer.
2. Remove alphabetical `assets_top` selection and fail closed unless an explicit
   point-in-time ranking source is supplied.
3. Add fail-loud provenance for `fjs`, `coarse`, `oracle`, and `sham` candidates,
   plus deterministic detector-contract tests that expose the current flat-zero
   failure.
4. Reconcile only necessary detector references from recovery by hash/manifest;
   never copy raw restricted data.
5. Run proportional native tests/lint, create a coherent verified commit, push
   the scoped branch, and verify remote commit/tree state.

Constraints: no direct main merge, full CRSP run, public release, synthetic
headline result, or silent estimator/treatment fallback.

