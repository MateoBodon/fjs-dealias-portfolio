# Results

## Result

- Isolated branch/worktree created from the exact pinned commit; the canonical
  dirty SSD worktree was not edited.
- Scientific contract frozen at
  `docs/strategy/FJS_SCIENTIFIC_RECENTER_PREDECLARATION.md`.
- Historical detector reference reconciled by exact hash at
  `docs/artifacts/detector-contract-reference/ticket24_week_full_fix/`; no raw
  restricted data entered Git.
- Candidate source is now mandatory and preserved through pre-gate, gating, and
  daily diagnostics. FJS/coarse/oracle/sham arms cannot be mixed.
- Alphabetical `assets_top` fallback was removed. A dated ranked source is
  required, validated, hashed, and written into run metadata.
- Unknown shrinker labels and repeated overlay eigendecomposition failures now
  raise instead of silently producing another estimator/result.
- The historical weekly curve still fails exactly where predeclared: zero
  detection and acceptance at every tested spike. No broad run is warranted.
- The 112-test native `unit or integration` gate passed. The narrower changed
  surface passed 78 focused tests with the same one intentional XFAIL.
- Run-log validation passed. The data-policy validator passed against the
  canonical SSD data files; the isolated worktree intentionally contains no
  ignored return-data copies.

## Claim boundary

This is an implementation and predeclaration milestone, not a real-data
performance result. Synthetic/semi-synthetic tests validate mechanism plumbing
only. T-012 remains coarse-attributed historical evidence.

## Continuation

Implement the independent deterministic MP-edge/root/component/reconstruction
reference harness, then pass the null, planted-power, invariance, and real-design
detector gates. Do not start a full CRSP run while the strict expected failure
remains.

Exact final commit/tree and remote readback are returned directly to the
Portfolio Administrator after push; no routine handoff/review bundle is made.
