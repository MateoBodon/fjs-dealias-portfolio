# Prompt

Resume the FJS deterministic reference-harness milestone in the existing
canonical in-project SSD worktree. Preserve prior milestone edits and remain the
sole FJS canonical writer.

Complete the bounded correctness/reference harness before any broad experiment:

1. Establish an independent deterministic edge/root/component/reconstruction
   oracle without importing the production FJS implementations.
2. Define deterministic, explicitly source-labeled oracle and sham behavior.
3. Audit the historical Ticket 24 treatment against the target component claim.
4. Add fail-loud checks and focused tests that distinguish verified reference
   behavior from known production mismatches.
5. Record concise evidence, run proportional native validation, commit, push the
   scoped branch, and verify remote commit/tree identity.

No memory-heavy empirical grid, direct main merge, restricted-data copy,
synthetic headline claim, or weakening of failed detector gates is authorized.
