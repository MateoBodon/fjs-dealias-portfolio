# Prompt

Resume the frozen FJS detector-reference milestone in the sole canonical SSD
writer worktree. Repair production against the independent oracle without
changing the oracle or weakening the reducer. Predeclare and execute only a
small provenance-valid between-component mechanism fixture, then stop before
any broad calibration or empirical experiment.

Required outcomes:

1. Correct the balanced one-way design, explicit-`C_s` equation (5.5) map,
   reachable theta roots, and spectral replacement semantics.
2. Convert all strict reference failures into ordinary passing tests.
3. Freeze the target-between fixture before implementation or execution and
   bind outputs to exact source, inputs, seeds, and hashes.
4. Make the unchanged deterministic reference reducer pass and prove byte
   reproduction.
5. Run proportional native validation, record the claim boundary, commit the
   coherent result, and publish the scoped branch with remote tree readback.

No broad synthetic grid, real-data headline claim, AWS launch, direct-main
merge, restricted-data copy, or weakening of a failed detector gate is
authorized.
