# Prompt

Resume the active Project OS v3 FJS M4 preparation goal from the existing v2
worktree. Preserve the exact Spark-low routing boundary: do not invent a route,
launch an LLM worker, or start AWS without a consumed authoritative admission.

Advance every safe deterministic part of the target-between calibration
package toward a paper-ready adaptive research result:

1. recover and validate the frozen manifest/runner work;
2. repair exact statistical, attribution, checkpoint, process, and cost-receipt
   defects without weakening the scientific stop-line;
3. prove real-kernel interrupt/resume equality and process cleanup;
4. make unbound scientific and AWS inputs fail closed;
5. run proportional native validation and commit a bounded milestone.

No broad calibration, WRDS experiment, headline result, AWS launch, push, or
threshold relaxation is authorized in this run.
