# Results

## Bounded integrity milestone

The M4 preparation surface is materially safer and scientifically honest:

- Exact 95% binomial intervals now have the correct upper root. For example,
  `0/6` yields `[0, 0.4592581264]`, not `[0, 1]`.
- Planted-direction and nuisance attribution use the actual directions that
  generated each panel. The previous code compared detections with an unrelated
  vector sampled after panel generation.
- The stable reducer records predeclared statistical checks separately from
  the still-unmet full detector gate. Smoke results cannot become calibration
  or empirical claims.
- Checkpoint resume binds manifest, scoped cell specifications, trial counts,
  environment, Git tree, and the exact executable-file fingerprint
  `3be1e68e4517eeb02e2351a41d729eadefbac686fe35225da6dcc81fd2efda2a`.
- Two-process interruption terminates and waits for remaining children. The
  final post-commit smoke left zero surviving workers and reproduced the fresh
  stable reducer exactly.
- Smoke or partial timing no longer fabricates a `c7i.16xlarge` cost estimate.
  A dollar receipt requires a fresh authoritative rate and an adequate
  full-shape or stratified benchmark.

## Frozen artifacts

- Full planning manifest: `calibration/manifests/fjs_m4_full_target_between_v2.json`
  - 252 cells;
  - file SHA-256
    `aa444e283fa99048e77353d7912c00baf5552c33cc777fc0b8137fe074448b22`;
  - manifest digest
    `12bd036c52166e03b3f55c2778a980a533fd1a779565d40e4f94d728ed3ae6cb`.
- Smoke manifest: `calibration/manifests/fjs_m4_smoke_target_between_v2.json`
  - 2 cells;
  - file SHA-256
    `ccca12d54fd73a0ea88e885297176c2de07153528af81083e8076960bd8cb5ef`;
  - manifest digest
    `5862b0af9a6eed8b1f8b2af731efabc8704c5a602c8e3c81490e5578d05615c9`.

Both artifacts reproduce byte-for-byte. The full artifact is a fail-closed
planning manifest, not an execution authorization.

## Honest smoke interpretation

The final two-cell, six-trial smoke proves only real-kernel orchestration and
stable reduction. It is deliberately underpowered for the frozen exact-size
and 80% power gates, and both cells fail the statistical stop-line. No positive
detector or paper claim is made from it.

## Exact blockers

Full calibration remains blocked by four explicit conditions:

1. the cell-specific independently computed detection boundary is unbound;
2. the invariance reducer is not yet hash-bound;
3. realistic CRSP-design calibration cells are not yet bound;
4. no fresh authoritative routed/AWS admission exists.

The trusted router returned no exact active-goal envelope, so Spark-low work was
not dispatched and the blockers were not bypassed. The next executable step is
to obtain the trusted Spark-low binding/admission for the bounded scientific
definition task, then freeze a new manifest generation rather than editing v2
after observing outcomes.
