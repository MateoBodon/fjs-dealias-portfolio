# Prompt

Continue the active FJS M4 goal from the exact published v3 tree. Implement an
additive successor v4 realistic-design input freezer and scientific contract
using the already-local CRSP dsf_v2 2013-2018 receipt chain.

The contract must bind exact source partition hashes, frozen CIZ filters,
PERMNO identity, lagged-cap membership, past-only FF5+MOM residualization, and
serialized/hash-bound missingness, aspect/group geometry, and residual
covariance inputs. Preserve v2/v3 byte identities, keep 2025 unopened, and do
not infer that the legacy ticker CSV descends from the newer raw receipts.

Because another portfolio lane owns the memory-heavy slot, run only
deterministic code/tests and one bounded local partition proof. Do not run the
full multi-partition derivation, AWS, detector outcomes, or empirical results.
