- Added a centralized gating reason vocabulary (`experiments/equity_panel/reasons.py`) and replaced the `guard_other` catch-all with explicit diagnostics (tvec_compute_error/target_zero/off_component, mu_nonfinite, etc.).
- `_run_single_period` now records `skip_reason_primary`, `skip_reason_detail`, and `exception_type`; diagnostic failures capture the exception type/message instead of collapsing into `guard_other`/`diagnostic_failure` with no context.
- `gating_diagnostics.csv`/`detection_summary.csv` emit the new fields and guard columns; `weekly_diagnostics.md` now reports primary-reason counts plus example windows per reason.
- Updated `tests/experiments/test_gating_diagnostics.py` to assert the new diagnostics (no `guard_other`, diagnostic_failure requires detail+exception type).
- Real-data smoke (deterministic) at `experiments/equity_panel/outputs_ticket-09_20251219_232717/oneway_J5_solver-auto_est-dealias_prep-prewhiten_modeoff/` produced `gating_diagnostics.csv` and `weekly_diagnostics.md` with detection rate 75%, single skip reason `no_isolated_spike`, and guard totals: tvec_compute_error=72, tvec_target_zero=2, tvec_off_component=1074 (no guard_other present).
