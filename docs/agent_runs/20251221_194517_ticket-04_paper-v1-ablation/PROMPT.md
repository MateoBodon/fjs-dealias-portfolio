Ticket to complete: Ticket #4 in docs/CODEX_SPRINT_TICKETS.md
“Minimal uncapped overlay vs shrinker ablation runner (paper‑v1 grid)”.

Goal:
Provide ONE pinned command/config (prefer Makefile target) that runs:
  shrinker ∈ {SCM, OAS, RIE} × overlay ∈ {OFF, ON}
uncapped (cap_active=false), and produces ONE combined table:
  reports/<root>/summary/paper_v1_ablation.csv
Optionally plots (DM bars + detection rates) under reports/<root>/summary/plots/.

Hard constraints:
- No fake fixes: do NOT mark runs uncapped by disabling caps detection or forcing flags.
- No silent solver fallbacks; do NOT use mv-skip except for smoke-only (and this ticket wants headline-eligible, so avoid mv-skip entirely).
- Prefer pinned config(s) committed; no “mystery settings” only in CLI history.
- Documentation-heavy: create a run log directory and update PROGRESS.md.

Before coding:
1) git status --porcelain; if dirty, DO NOT revert user changes; just avoid touching unrelated files.
2) Create feature branch: feat/ticket-04-paper-v1-ablation
3) Create run log dir:
   docs/agent_runs/<RUN_NAME>/ where RUN_NAME = YYYYMMDD_HHMMSS_ticket-04_paper-v1-ablation
   Populate PROMPT/COMMANDS/RESULTS/TESTS/META as you work (append as you go).

Implementation tasks:
A) Add pinned config:
- Create experiments/eval/config.paper_v1.yaml (or an equivalent committed config) that pins:
  - window, horizon, assets_top
  - gating defaults for overlay ON runs (and make overlay OFF an explicit, documented toggle)
  - min_comparison_windows consistent with PLAN_OF_RECORD
  - mv_gamma/mv_tau, mv_box, turnover_bps, etc. (whatever the current pipeline expects)
- The config must be used by the new make target and recorded in run.json/resolved config output.

B) Add runner entrypoint:
- Update Makefile with a new target: rc-paper-v1-ablate
  - Creates a fresh root dir: reports/rc-paper-v1-ablate-<timestamp>/
  - Runs 6 eval runs into subdirs, e.g.:
      scm_off, scm_on, oas_off, oas_on, rie_off, rie_on
    using python -m experiments.eval.run with:
      --config experiments/eval/config.paper_v1.yaml
      --shrinker <scm|oas|rie>
      <overlay OFF toggle> for *_off
      <overlay ON toggle> for *_on
    NO --max-windows. NO date truncation caps. NO mv-skip.
  - After the 6 runs:
      PYTHONPATH=src:. python tools/make_summary.py --rc-dir <root>
  - Then create the combined table:
      PYTHONPATH=src:. python tools/paper_v1_ablation.py --rc-dir <root>
    (you will implement tools/paper_v1_ablation.py)

C) Implement tools/paper_v1_ablation.py:
- Input: --rc-dir <root>
- Reads root/summary/summary_perf.csv and root/summary/summary_detection.csv (generated by tools/make_summary.py)
- Produces root/summary/paper_v1_ablation.csv with one row per (shrinker, overlay_flag) at minimum.
  Include columns that make validity auditable:
    - shrinker, overlay_flag
    - cap_active, cap_sources (should be false/empty for all 6)
    - windows_evaluated, window_coverage
    - comparison_valid_mse/qlike + n_effective_* (or the equivalents in summary files)
    - detection_rate_* (mean) and changed_share_* if available
    - delta_mse_vs_baseline / delta_qlike_vs_baseline for overlay ON runs (NaN allowed for OFF)
- If plots are easy, write them to root/summary/plots/ (but do not block the ticket on plotting).

D) Tests:
- Add a CI-safe unit test for tools/paper_v1_ablation.py:
  - construct a tiny fake summary_perf.csv + summary_detection.csv in tmp_path
  - run the tool
  - assert output CSV exists and has expected rows/columns.
- Run: make test-fast (required).

Validation runs (required; smallest sufficient real-data smoke):
- Run rc-paper-v1-ablate in deterministic mode using a SMALL REAL dataset already in the repo.
  Prefer the repo’s small derived returns/factors CSVs (do not create + commit large data).
- Verify in RESULTS.md with grep/snippets:
  - Each of the 6 run subdirs has run.json with cap_active=false and cap_sources empty
  - The root summary tables are non-empty (or if empty, explain why; empty is a FAIL unless genuinely no windows exist)
  - reports/<root>/summary/paper_v1_ablation.csv exists and is non-empty
  - limitations.md does NOT list the 6 runs as excluded smoke-only.

Docs updates (required):
- Update PROGRESS.md with:
  - commands run
  - output root path
  - where paper_v1_ablation.csv is
  - a 2–4 bullet “what we learned” (even if neutral)
- Update docs/CODEX_SPRINT_TICKETS.md:
  - mark Ticket #1 as DONE (it’s already implemented per PROGRESS)
  - keep Ticket #2 DONE
  - mark Ticket #4 as DONE only if acceptance criteria met; otherwise leave as IN PROGRESS and explain.

Commits:
- Make small logical commits.
- Every commit message body must include: Tests run: <exact commands>

Finish:
- Ensure docs/agent_runs/<RUN_NAME>/ has PROMPT/COMMANDS/RESULTS/TESTS/META.
- Generate bundle:
    make gpt-bundle TICKET=ticket-04 RUN_NAME=<RUN_NAME>
- Record the bundle path in docs/agent_runs/<RUN_NAME>/RESULTS.md.

Do not merge to main. Stop after bundle generation.
If something blocks completion (e.g., solver missing), do NOT hack around with mv-skip; instead fail loudly in RESULTS.md and still produce the bundle with a clear FAIL.
