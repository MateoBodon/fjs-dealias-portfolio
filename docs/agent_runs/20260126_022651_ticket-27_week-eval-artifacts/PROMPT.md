# Prompt

Ticket: FJS-TKT-027
Goal: Make week RC eval runs always produce run.json/resolved_config and a non-empty run.log, even on early exit after prewhiten.
Scope: Update experiments/eval/run.py, add regression test tests/experiments/test_eval_week_artifacts.py, update PROGRESS.md + docs/agent_runs log, generate GPT bundle.
