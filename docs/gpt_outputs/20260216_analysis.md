# 2026-02-16 External Analysis Snapshot

Source note: This file captures the analysis context provided for Ticket 31 in this run. No separate `Analysis.md` file was present in the workspace at execution time.

## Executive view

The repository has strong engineering discipline and reproducibility controls:
- ticketed changes with run logs
- explicit stop-the-line guardrails
- test and bundle gates
- strong artifact provenance

The research conclusion is still open. The core blocker is not pipeline quality; it is weak empirical signal for the overlay under real-data conditions.

## What is currently working

- Auditability and reproducibility workflow is operational (`docs/agent_runs/`, `PROGRESS.md`, bundle tooling).
- Core test gate and run-log validation are wired into Make targets.
- Daily/weekly runners, synthetic calibration harnesses, and summary tooling produce structured outputs with run metadata.

## Main research blockers

1. Injection sensitivity remains flat-zero on real windows for the primary week design in the latest documented injection run.
2. Real-data effect size is still not advisor-ready as a headline claim because treatment activation and practical impact are unclear in the windows that matter.
3. Publishability risk remains high until detection math and financial residual behavior are reconciled.

## Priority gates (in order)

1. Injection flat-zero debugging:
   - show non-flat response for at least one design/edge mode, or
   - provide a conclusive explanation for why theoretical detection does not translate to the financial residual process.
2. One advisor-ready uncapped run:
   - complete/uncapped (`cap_active=false`)
   - valid aligned comparisons (`comparison_valid_*` true with meaningful `n_effective_*`)
   - clear detection/acceptance/skip accounting
   - summary tables safe to cite.

## Scope control recommendation

Do not expand the experiment grid until the two gates above are met. Additional sweeps without a non-flat sensitivity result are unlikely to improve credibility.
