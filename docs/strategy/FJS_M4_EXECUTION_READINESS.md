# FJS M4 Execution Readiness

status: deterministic orchestration integrity passed; full scientific execution blocked
as_of: 2026-07-11
active_goal: `goal_71e41ee4ddc6`

## Ready now

- Distinct full and smoke manifest identities and byte-stable generation.
- Correct exact-binomial interval reducer and explicit predeclared checks.
- Target-between planted/nuisance direction attribution.
- Exact code-byte, manifest, cell, trial, and environment checkpoint binding.
- Real process-parallel interruption, cleanup, resume, and stable reduction.
- Smoke artifacts prohibited from canonical repository paths.
- Cost reporting refuses smoke-to-full extrapolation.

## Stop-line blockers

The v2 full manifest has `full_execution_ready=false` and
`aws_execution_authorized=false`. The runner exits before creating a run root
until all of the following are frozen in a new generation:

1. a cell-specific independent FJS detection-boundary implementation;
2. a hash-bound reducer for standardized rescaling, deterministic row order,
   asset permutation, and group-label permutation;
3. realistic-design cells sampled from development-only CRSP window geometry
   and residual covariance, with exact licensed-source manifests;
4. a consumed trusted route and a fresh AWS launch admission/cost receipt.

The current trusted `route-plan` returns no exact project/goal task envelope.
Quota diagnostics or general AWS standing authority do not replace that
missing admission.

## Next executable sequence

1. Through the trusted Spark-low route, define and independently validate the
   cell-specific detection boundary and invariance comparison tolerance without
   observing full-run outcomes.
2. Bind development-only realistic-design cell inputs; keep 2025 unopened.
3. Freeze a new manifest version. Do not edit v2 in place.
4. Run the separate smoke profile outside the repository and require exact
   interrupt/resume/fresh equality.
5. Obtain a current `c7i.16xlarge` price and perform a stratified full-shape
   timing benchmark. Require July actual plus planned incremental cost to remain
   within the active `$250` account boundary.
6. Only after authoritative admission, launch from
   `project-os-general-worker-v1` with profile `portfolio-admin`, SSM-only
   access, no inbound rules, encrypted storage, and tags `Project`, `RunId`,
   `Owner`, `ManagedBy`, and `Purpose`.
7. Persist permitted checkpoints/results under `projects/fjs/`, verify S3
   readback and hashes, then stop or terminate only after confirming no unique
   valuable state remains on the worker.

No AWS command is executable from this document while the v2 readiness flags
remain false.
