[pytest]
markers =
    unit: small, deterministic unit tests (default fast suite)
    integration: multi-module tests (smoke size)
    slow: larger slices/ablations
    heavy: anything near full jobs
addopts = -q -m "not slow"
timeout = 60
